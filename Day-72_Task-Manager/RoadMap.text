🚀 🔹 Full Project Roadmap: Task Manager App

🔥 🧱 PHASE 1: Project Setup

✅ What you’ll do:
Create project folder
Initialize npm
Install dependencies
Setup basic Express server
🛠️ Tech:
Node.js
Express
npm

---------------------------------------
🚀 🔹 PHASE 1: Project Setup
---------------------------------------
🔥 1. Create Project Folder

Open terminal and run:
mkdir task-manager
cd task-manager

🔥 2. Initialize Node Project
npm init -y
👉 This creates:
package.json

🔥 3. Install Required Packages
npm install express
npm install nodemon -D

👉 Why:
express → backend framework
nodemon → auto restart server

🔥 4. Create Basic Files

Create this structure:
task-manager/
│
├── node_modules/
├── package.json
├── package-lock.json
└── server.js

🔥 5. Setup Basic Express Server
👉 Open server.js and write:
const express = require("express");
const app = express();

// basic route
app.get("/", (req, res) => {
  res.send("Task Manager API Running 🚀");
});

// server start
const PORT = 3000;

app.listen(PORT, () => {
  console.log(`Server running on port ${PORT}`);
});

🔥 6. Add Scripts (IMPORTANT)

👉 Open package.json and update:

"scripts": {
  "start": "node server.js",
  "dev": "nodemon server.js"
}

🔥 7. Run Server
npm run dev

👉 Open browser:

http://localhost:3000

👉 You should see:

Task Manager API Running 🚀

⚠️ MUST KNOW (REAL PROJECT)
🔸 1. Always use nodemon in dev

👉 Saves time (auto restart)

🔸 2. Server must be running

👉 Otherwise API won’t work ❌

🔸 3. Port conflicts

👉 If 3000 not working:

const PORT = 5000;
🔸 4. Basic debug

If error:
npm install
---------------------------------------

🔥 ⚙️ PHASE 2: Backend Core (Express)
✅ What you’ll do:
Setup server
Create routes
Handle req & res
Add middleware
🧠 Concepts Covered:
Routing
Middleware
req/res
Error handling

---------------------------------------
🚀 🔹 PHASE 2: Backend Core (Express)
---------------------------------------

🔹 1. Update Project Structure (IMPORTANT)

👉 Create folders:

mkdir routes controllers middleware

👉 Final structure:

task-manager/
│
├── node_modules/
├── routes/
├── controllers/
├── middleware/
├── server.js
├── package.json

🔥 2. Create Basic Route File

👉 Create file:

routes/taskRoutes.js
Add this:
const express = require("express");
const router = express.Router();

// test route
router.get("/", (req, res) => {
  res.send("Task route working");
});

module.exports = router;

🔥 3. Connect Routes to Server

👉 Update server.js

const express = require("express");
const app = express();

// middleware (VERY IMPORTANT)
app.use(express.json());

// import routes
const taskRoutes = require("./routes/taskRoutes");

// use routes
app.use("/tasks", taskRoutes);

// basic route
app.get("/", (req, res) => {
  res.send("API Running");
});

const PORT = 3000;

app.listen(PORT, () => {
  console.log(`Server running on port ${PORT}`);
});

👉 Test:

http://localhost:3000/tasks

🔥 4. Create Controller (Separation of Logic)

👉 Create:

controllers/taskController.js
Add:
// get all tasks
exports.getTasks = (req, res) => {
  res.json([
    { id: 1, title: "Learn Node" },
    { id: 2, title: "Build Project" }
  ]);
};

🔥 5. Connect Controller to Route

👉 Update routes/taskRoutes.js

const express = require("express");
const router = express.Router();

const { getTasks } = require("../controllers/taskController");

// route using controller
router.get("/", getTasks);

module.exports = router;

👉 Now test:

http://localhost:3000/tasks

👉 You’ll get JSON ✅

🔥 6. Add Middleware (IMPORTANT 🔥)

👉 Create:

middleware/logger.js
Add:
const logger = (req, res, next) => {
  console.log(`${req.method} ${req.url}`);
  next();
};

module.exports = logger;

👉 Use it in server.js:

const logger = require("./middleware/logger");

app.use(logger);

👉 Now every request logs:

GET /tasks

🔥 7. Add Error Handling Middleware

👉 In server.js (at bottom):

// 404 handler
app.use((req, res) => {
  res.status(404).json({ message: "Route not found" });
});

// error handler
app.use((err, req, res, next) => {
  res.status(500).json({
    error: err.message
  });
});

⚠️ MUST KNOW (REAL PROJECT)
🔸 1. Separation of concerns 🔥

👉 Routes → define endpoints
👉 Controllers → handle logic
👉 Middleware → reusable logic

🔸 2. Always use express.json()

👉 Without it:
❌ POST data won’t work

🔸 3. Always structure project early

👉 Avoid messy code later

🔸 4. Middleware order matters

👉 Logger → Routes → Error handler

🔸 5. Use controllers always

❌ Don’t write logic in routes
✔ Keep it clean
---------------------------------------

🔥 🗄️ PHASE 3: Database Integration
✅ What you’ll do:
Connect database
Create schema/model
Store tasks
🛠️ Tech:
MongoDB
Mongoose

---------------------------------------
🚀 🔹 PHASE 3: Database Integration (MySQL)
---------------------------------------

🔥 1. What We Will Do
Install MySQL package
Connect Node.js to MySQL
Create database
Create tasks table
Test connection

🔹 2. Install MySQL Package
npm install mysql2

👉 We use mysql2 (modern + faster)

🔥 3. Create Database in MySQL

Open MySQL and run:

CREATE DATABASE task_manager;

🔹 4. Create Table
USE task_manager;

CREATE TABLE tasks (
  id INT AUTO_INCREMENT PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  description TEXT,
  status VARCHAR(50) DEFAULT 'pending',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

🔥 5. Create DB Connection File

👉 Create file:

config/db.js
Add this:
const mysql = require("mysql2");

// create connection
const db = mysql.createConnection({
  host: "localhost",
  user: "root",
  password: "your_password", // change this
  database: "task_manager"
});

// connect
db.connect((err) => {
  if (err) {
    console.log("Database connection failed:", err);
  } else {
    console.log("MySQL Connected ✅");
  }
});

module.exports = db;

🔥 6. Connect DB in Server

👉 In server.js

require("./config/db");

👉 Put this at top

🔥 7. Run Server
npm run dev

👉 You should see:

MySQL Connected ✅

🔥 8. Test Query (VERY IMPORTANT)

👉 Update taskController.js

const db = require("../config/db");

exports.getTasks = (req, res) => {
  db.query("SELECT * FROM tasks", (err, result) => {
    if (err) {
      return res.status(500).json({ error: err.message });
    }
    res.json(result);
  });
};

👉 Test:

http://localhost:3000/tasks

👉 You’ll get data from DB 🎉
---------------------------------------

🔥 🔄 PHASE 4: CRUD API (Core Backend)
✅ What you’ll build:
POST   /tasks       → create task
GET    /tasks       → get all tasks
GET    /tasks/:id   → get single task
PUT    /tasks/:id   → update task
DELETE /tasks/:id   → delete task
🧠 Concepts:
REST API
Data validation
Error handling

---------------------------------------
🚀 🔹 PHASE 4: CRUD API (MySQL + Express)
---------------------------------------

👉 We will build full API:

POST   /tasks        → create task
GET    /tasks        → get all tasks
GET    /tasks/:id    → get single task
PUT    /tasks/:id    → update task
DELETE /tasks/:id    → delete task

🔥 1. Update Controller (MAIN LOGIC)

👉 Open: controllers/taskController.js

🔸 1. Create Task (POST)
const db = require("../config/db");

// CREATE
exports.createTask = (req, res) => {
  const { title, description } = req.body;

  const sql = "INSERT INTO tasks (title, description) VALUES (?, ?)";

  db.query(sql, [title, description], (err, result) => {
    if (err) return res.status(500).json({ error: err.message });

    res.status(201).json({
      message: "Task created",
      taskId: result.insertId
    });
  });
};
🔸 2. Get All Tasks (GET)
// READ ALL
exports.getTasks = (req, res) => {
  db.query("SELECT * FROM tasks", (err, result) => {
    if (err) return res.status(500).json({ error: err.message });

    res.json(result);
  });
};
🔸 3. Get Single Task
// READ ONE
exports.getTaskById = (req, res) => {
  const { id } = req.params;

  db.query("SELECT * FROM tasks WHERE id = ?", [id], (err, result) => {
    if (err) return res.status(500).json({ error: err.message });

    if (result.length === 0) {
      return res.status(404).json({ message: "Task not found" });
    }

    res.json(result[0]);
  });
};
🔸 4. Update Task
// UPDATE
exports.updateTask = (req, res) => {
  const { id } = req.params;
  const { title, description, status } = req.body;

  const sql = `
    UPDATE tasks 
    SET title = ?, description = ?, status = ?
    WHERE id = ?
  `;

  db.query(sql, [title, description, status, id], (err, result) => {
    if (err) return res.status(500).json({ error: err.message });

    res.json({ message: "Task updated" });
  });
};
🔸 5. Delete Task
// DELETE
exports.deleteTask = (req, res) => {
  const { id } = req.params;

  db.query("DELETE FROM tasks WHERE id = ?", [id], (err, result) => {
    if (err) return res.status(500).json({ error: err.message });

    res.json({ message: "Task deleted" });
  });
};

🔥 2. Update Routes

👉 Open: routes/taskRoutes.js

const express = require("express");
const router = express.Router();

const {
  createTask,
  getTasks,
  getTaskById,
  updateTask,
  deleteTask
} = require("../controllers/taskController");

// CRUD routes
router.post("/", createTask);
router.get("/", getTasks);
router.get("/:id", getTaskById);
router.put("/:id", updateTask);
router.delete("/:id", deleteTask);

module.exports = router;

🔥 3. Test API (VERY IMPORTANT)

Use:

Postman OR browser (for GET)
🔸 Create Task
POST http://localhost:3000/tasks

Body (JSON):

{
  "title": "Learn Backend",
  "description": "Complete Node project"
}
🔸 Get All
GET http://localhost:3000/tasks
🔸 Get One
GET http://localhost:3000/tasks/1
🔸 Update
PUT http://localhost:3000/tasks/1
{
  "title": "Updated Task",
  "description": "Updated desc",
  "status": "completed"
}
🔸 Delete
DELETE http://localhost:3000/tasks/1
⚠️ MUST KNOW (REAL PROJECT)
🔸 1. Always validate input
if (!title) {
  return res.status(400).json({ error: "Title required" });
}
🔸 2. Use parameterized queries
WHERE id = ?

👉 Prevent SQL injection 🔥

🔸 3. Always check result
if (result.length === 0)
🔸 4. Proper status codes
201 → created
200 → success
404 → not found
500 → server error
🔸 5. Never trust frontend data

👉 Always validate in backend
---------------------------------------

🔥 🎨 PHASE 5: Frontend (Simple UI)
✅ What you’ll do:
Create form to add task
Display tasks
Update & delete buttons
🛠️ Tech:
HTML
CSS
JavaScript (Fetch API)

---------------------------------------
🚀 🔹 PHASE 5: Frontend (HTML + CSS + JS)

👉 Goal:

Create UI
Connect with your API
Perform CRUD from browser

🔥 1. Create Frontend Folder

Inside your project:

mkdir public

👉 Structure:

task-manager/
│
├── public/
│   ├── index.html
│   ├── style.css
│   └── script.js

🔥 2. Serve Frontend from Express

👉 In server.js add:

app.use(express.static("public"));

👉 Now your frontend runs at:

http://localhost:3000

🔥 3. Create HTML (UI)

👉 public/index.html

<!DOCTYPE html>
<html>
<head>
  <title>Task Manager</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>

  <h1>Task Manager 🚀</h1>

  <!-- Form -->
  <form id="taskForm">
    <input type="text" id="title" placeholder="Title" required>
    <input type="text" id="description" placeholder="Description">
    <button type="submit">Add Task</button>
  </form>

  <h2>Tasks</h2>
  <ul id="taskList"></ul>

  <script src="script.js"></script>
</body>
</html>

🔥 4. Add Basic CSS (Optional but clean)

👉 public/style.css

body {
  font-family: Arial;
  padding: 20px;
}

input, button {
  margin: 5px;
  padding: 8px;
}

li {
  margin: 10px 0;
}

🔥 5. JavaScript (CONNECT BACKEND 🔥)

👉 public/script.js

🔸 1. Load Tasks
const API = "http://localhost:3000/tasks";

async function loadTasks() {
  const res = await fetch(API);
  const tasks = await res.json();

  const list = document.getElementById("taskList");
  list.innerHTML = "";

  tasks.forEach(task => {
    const li = document.createElement("li");
    li.innerHTML = `
      <b>${task.title}</b> - ${task.description}
      <button onclick="deleteTask(${task.id})">❌</button>
    `;
    list.appendChild(li);
  });
}

loadTasks();

🔸 2. Add Task
document.getElementById("taskForm").addEventListener("submit", async (e) => {
  e.preventDefault();

  const title = document.getElementById("title").value;
  const description = document.getElementById("description").value;

  await fetch(API, {
    method: "POST",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify({ title, description })
  });

  loadTasks();
});

🔸 3. Delete Task
async function deleteTask(id) {
  await fetch(`${API}/${id}`, {
    method: "DELETE"
  });

  loadTasks();
}

🔥 6. Run Project
npm run dev

👉 Open:

http://localhost:3000
🎉 WHAT YOU CAN DO NOW
✅ Add task
✅ View tasks
✅ Delete tasks
⚠️ MUST KNOW (REAL PROJECT)
🔸 1. Frontend talks to backend via API
JS → fetch() → Express → DB → Response
🔸 2. Always use JSON
headers: { "Content-Type": "application/json" }
🔸 3. Reload UI after changes

👉 Call:

loadTasks();
🔸 4. Async/Await is IMPORTANT

👉 Used for API calls

🔸 5. Debug using browser console

👉 Press:

F12 → Console
---------------------------------------

🔥 🔗 PHASE 6: Frontend + Backend Integration
✅ What you’ll do:
Connect frontend with API
Handle API responses
Show real-time updates

---------------------------------------
🚀 🔹 PHASE 6: Update Feature + UI Improvement
🔥 1. Add Update Feature (IMPORTANT)

👉 Right now you can:

add ✅
delete ✅
view ✅
❌ update missing
🔸 Step 1: Update UI

👉 Modify script.js inside loadTasks()

Replace this part:

li.innerHTML = `
  <b>${task.title}</b> - ${task.description}
  <button onclick="deleteTask(${task.id})">❌</button>
`;

👉 With this:

li.innerHTML = `
  <b>${task.title}</b> - ${task.description} (${task.status})
  <button onclick="deleteTask(${task.id})">❌</button>
  <button onclick="editTask(${task.id}, '${task.title}', '${task.description}')">✏️</button>
`;
🔥 2. Add Edit Function
function editTask(id, title, description) {
  document.getElementById("title").value = title;
  document.getElementById("description").value = description;

  window.editId = id; // store id
}
🔥 3. Modify Form Submit (Create + Update)

👉 Replace your submit code:

document.getElementById("taskForm").addEventListener("submit", async (e) => {
  e.preventDefault();

  const title = document.getElementById("title").value;
  const description = document.getElementById("description").value;

  if (window.editId) {
    // UPDATE
    await fetch(`${API}/${window.editId}`, {
      method: "PUT",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        title,
        description,
        status: "completed"
      })
    });

    window.editId = null;
  } else {
    // CREATE
    await fetch(API, {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({ title, description })
    });
  }

  document.getElementById("taskForm").reset();
  loadTasks();
});
🔥 4. Improve UI (Make it Look Better)

👉 Update style.css

body {
  font-family: Arial;
  background: #f5f5f5;
  padding: 20px;
}

h1 {
  text-align: center;
}

form {
  display: flex;
  gap: 10px;
  justify-content: center;
}

ul {
  list-style: none;
  padding: 0;
}

li {
  background: white;
  padding: 10px;
  margin: 10px auto;
  width: 60%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-radius: 5px;
}

button {
  margin-left: 5px;
  cursor: pointer;
}
🔥 5. Add Basic Validation (IMPORTANT)

👉 In submit:

if (!title) {
  alert("Title is required");
  return;
}
🔥 6. Add Loading UX (Small but Powerful)
async function loadTasks() {
  const list = document.getElementById("taskList");
  list.innerHTML = "Loading...";

  const res = await fetch(API);
  const tasks = await res.json();

  list.innerHTML = "";

  tasks.forEach(task => {
    const li = document.createElement("li");
    li.innerHTML = `
      <b>${task.title}</b> - ${task.description}
      <button onclick="deleteTask(${task.id})">❌</button>
      <button onclick="editTask(${task.id}, '${task.title}', '${task.description}')">✏️</button>
    `;
    list.appendChild(li);
  });
}
---------------------------------------

🔥 ⚠️ PHASE 7: Real Project Improvements
✅ Add:
Loading states
Error messages
Clean UI
Validation

---------------------------------------
🚀 🔹 PHASE 7: Production-Ready Setup
🔥 1. Environment Variables (.env) 🔐

👉 Right now you have DB credentials in code ❌
👉 This is unsafe

🔸 Install dotenv
npm install dotenv
🔸 Create .env file
PORT=3000

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=task_manager
🔸 Update DB config (config/db.js)
require("dotenv").config();
const mysql = require("mysql2");

const db = mysql.createConnection({
  host: process.env.DB_HOST,
  user: process.env.DB_USER,
  password: process.env.DB_PASSWORD,
  database: process.env.DB_NAME
});

db.connect((err) => {
  if (err) {
    console.log("DB Error:", err);
  } else {
    console.log("MySQL Connected ✅");
  }
});

module.exports = db;
🔸 Update server.js
require("dotenv").config();

const PORT = process.env.PORT || 3000;
🔥 2. Ignore Sensitive Files

👉 Create .gitignore

node_modules/
.env
🔥 3. Better Folder Structure (IMPORTANT)

👉 Update structure:

task-manager/
│
├── config/
│   └── db.js
├── controllers/
├── routes/
├── middleware/
├── public/
├── .env
├── .gitignore
├── server.js
└── package.json
🔥 4. Add Global Error Handler (Clean)

👉 Improve error handling:

app.use((err, req, res, next) => {
  console.error(err);

  res.status(err.status || 500).json({
    success: false,
    message: err.message || "Server Error"
  });
});
🔥 5. Add CORS (Frontend Safety)
npm install cors
const cors = require("cors");
app.use(cors());
🔥 6. Add Basic Logging (Already Done 👍)

👉 You already created logger middleware — good practice

🔥 7. Code Cleanup (VERY IMPORTANT)

👉 Make sure:

No duplicate code
Clean variable names
Remove console.logs (except important ones)
🔥 8. Optional: Better Response Format

👉 Instead of raw data:

res.json({
  success: true,
  data: result
});
🔥 9. Deployment Awareness (Concept)

👉 You don’t need to deploy now, but know:

Backend → Render / Railway
Frontend → Netlify / GitHub Pages
---------------------------------------
